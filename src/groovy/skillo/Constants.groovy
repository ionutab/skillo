package skillo

class Constants {

    public static final int QUALIFICATION_EXPIRE_PERIOD = 2; // the number of months before the expiry of a qualification when it will be considered unreliable

    public static final int DEFAULT_PAYROLL_COMPANY_ID = 1; // the default payroll company id for SMR = FOSRE so they will always have this payroll company selected


}
